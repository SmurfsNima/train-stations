# PANTOhealth — Train Stations (Frontend Assignment)

## Features
- Fetches stations from a public JSON endpoint
- Loading + error states
- Leaflet map with station markers
- City filter that updates both list and map
- Clicking a station selects it and zooms to it on the map
- Includes a test for filtering behavior

## Tech
- React + TypeScript + Vite
- TailwindCSS
- Leaflet + React-Leaflet
- Vitest + React Testing Library

## Setup
```bash
npm install
npm run dev
